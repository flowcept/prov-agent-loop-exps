profile: local_smoke
description: Small desktop-scale run through tutorial step 2, before per-epoch training analysis.
repetitions: 3
run_root: runs/local
constraints:
  max_requested_nodes: 10
  campaign_wall_time_minutes: 30
search_config_hint:
  description: The code assistant should generate runs/local/<run_id>/search_config.yaml for each run.
  required_sections:
    - objective
    - constraints
    - search_space_rationale
    - generated_run_artifacts
    - evaluation_plan
