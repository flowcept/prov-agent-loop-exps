# Local OPL Machine Learning Workflow Prompt

You are working in the local experiment repository. Create or reuse scripts needed to run the Flowcept LLM tutorial through step 2 only:

- Step 1: search workflow only.
- Step 2: data preparation plus search workflow.

Use the local smoke hyperparameter grid from `project.yaml`. Do not add per-epoch instrumentation, parent-forward instrumentation, child-layer instrumentation, or Frontier/Slurm scripts in this run.

Run the workflow, record the commands used, preserve generated logs and manifests, and stop after step 2 succeeds or after writing a clear failure note.
