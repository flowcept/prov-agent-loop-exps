# Frontier Run Packages

Place exported Frontier packages here after they are returned by the Frontier Codex session.

Each package should follow `/Users/valesca/Documents/projects/github.com/flowcept/prov-agent-loop-exps/docs/artifact_contract.md` and can be imported locally with:

```bash
.venv/bin/python scripts/capture/import_run.py --package-dir runs/frontier/<package> --mongo-db <local_db_name>
```
